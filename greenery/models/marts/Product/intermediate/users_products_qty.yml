version: 2

models:

  - name: users_products_qty
    description: Table containing products infortion (viwed, added to chart, purchased) in events made by customers
    columns:
      - name: user_id
        description: Unique user ID in the platform
      - name: product_id
        description: Product id
      - name: qty_purchased
        description: Quantity ppurchased by user ever